# Shared trajectory engine

`trajectory_core.py` is the finite-pool solver shared by
`maintext_Fig5/` (main-text Figure 5) and `suppl_FigS8/`
(Supplementary Material 1, Fig. S8). Both import it by relative path, so
this folder must stay a sibling of theirs.

It was previously named `figures8_9_shared/`.
